const { Employment } = require("./employment.model");
const { Certificate, Experience } = require("./profession.model");
const { Teacher } = require("./teacher.model");

module.exports = {
  Employment,
  Certificate,
  Experience,
  Teacher,
};
